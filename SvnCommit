# Split ExpForExample.cpp to ExpApxFor.omh and ExpApxFor.cpp.
#
# SvnCommit: file that made this commit plus comments about adding directories.
# ExpApxFor.cpp: move ExpForExample.cpp source code to here.
# ExpApxSeq.cpp: change the section title.
# ExpForExample.cpp: split into ExpApxFor.omh and ExpApxFor.cpp.
# ExpRevExample.cpp: fix cross references that changed.
# Makefile.am: map ExpForExample -> ExpApxFor.
# ExpApxFor.omh: move ExpForExample.cpp documentation to here.
# ExpApxSeq.omh: change title and some headings, groupt operation with symbol.
# Introduciton.omh: change ExpForExample.cpp to ExpApxFor.omh. 
#
add_list="
	Introduction/ExpApxFor.cpp
	omh/ExpApxFor.omh
"
change_list="
	SvnCommit
	Introduction/ExpApxSeq.cpp
	Introduction/ExpRevExample.cpp
	Introduction/Makefile.am
	omh/ExpApxSeq.omh
	omh/Introduction.omh
"
delete_list="
	Introduction/ExpForExample.cpp
"
#----------------------------------------------------------------------
# Edit section above for individual cases, section below stays the same
# Do not use add_list to add directories; use the following instead:
#	svn add --non-recursive dir
#	svn commit directory -m "adding directory dir" dir
# where dir is replaced by the name of the directory. 
# Then use add_list to add the files within that directory.
#----------------------------------------------------------------------
sed -e "/^[^#]/d" -e "s/^[#] *//" < SvnCommit > svn.log
count=0
for file in $add_list $change_list
do
	count=`expr $count + 1`
	ext=`echo $file | sed -e "s/.*\././"`
	if \
	[ $file != "CheckIncludeFile"  ] && \
	[ $file != "CheckIncludeOmh"   ] && \
	[ $file != "SvnCommit"         ] && \
	[ $file != "SvnCommit.sed"     ] && \
	[ $ext  != ".vcproj"           ] && \
	[ $ext  != ".vim"              ]
	then
		# automatic edits and backups
		echo "cp $file junk.$count"
		cp $file junk.$count
		sed -f SvnCommit.sed < junk.$count > $file
		diff junk.$count $file
	fi
done
for file in $add_list
do
	echo "svn add $file ?"
done
for file in $delete_list
do
	echo "svn delete $file ?"
done
response=""
while [ "$response" != "c" ] && [ "$response" != "a" ]
do
	read -p "continue [c] or abort [a] ? " response
done
if [ "$response" = "c" ]
then
	for file in $add_list
	do
		svn add $file
	done
	for file in $delete_list
	do
		svn delete $file
	done
	svn commit --file svn.log $add_list $change_list $delete_list
else
	count=0
	for file in $add_list $change_list
	do
		count=`expr $count + 1`
		ext=`echo $file | sed -e "s/.*\././"`
		if \
		[ $file != "CheckIncludeFile"  ] && \
	   	[ $file != "CheckIncludeOmh"   ] && \
	   	[ $file != "SvnCommit"         ] && \
	   	[ $file != "SvnCommit.sed"     ] && \
	   	[ $ext  != ".vcproj"           ] && \
	   	[ $ext  != ".vim"              ]
		then
			# undo the automatic edits
			echo "mv junk.$count $file"
			mv junk.$count $file
		fi
	done
fi
